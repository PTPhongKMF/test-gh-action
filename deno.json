{
  "name": "zephyr-release",
  "version": "0.1.0",
  "description": "Zephyr Release is yet another automated tool for version bumping, changelog generation, and release publishing. It follows Conventional Commits and uses Semantic Versioning for version numbers but skips strict bump rules in favor of a more flexible approach. Designed for my personal use, but may also be useful to others.",
  "license": "Apache-2.0",

  "exports": "./README.md",

  "tasks": {
    "prebuild": "deno run --allow-run --allow-write scripts/prebuild.ts",
    "build": {
      "command": "tsdown",
      "dependencies": ["prebuild"]
    }
  },

  "nodeModulesDir": "auto",
  "imports": {
    "@actions/core": "npm:@actions/core@^2.0.1",
    "@actions/github": "npm:@actions/github@^6.0.1",

    "@std/path": "jsr:@std/path@^1.1.3",
    "tsdown": "npm:tsdown@^0.18.2"
  },

  "exclude": [
    "dist"
  ],

  "compilerOptions": {
    "lib": ["deno.ns"]
  }
}
